local:
  hosts:
    localhost:

ceph:
  vars:
    ansible_user: ubuntu
  hosts:
    mk8s-1.local:
    mk8s-2.local:
    mk8s-3.local:

ceph-bootstrap:
  vars:
    ansible_user: ubuntu
  hosts:
    mk8s-1.local:

ceph-nodes:
  vars:
    ansible_user: ubuntu
  hosts:
    mk8s-2.local:
    mk8s-3.local:

microk8s:
  vars:
    ansible_user: ubuntu
  hosts:
    mk8s-1.local:
    mk8s-2.local:
    mk8s-3.local:

microk8s-bootstrap:
  vars:
    ansible_user: ubuntu
  hosts:
    mk8s-1.local:

microk8s-nodes:
  vars:
    ansible_user: ubuntu
  hosts:
    mk8s-2.local:
    mk8s-3.local: